from setuptools import setup

setup(
    name='cow',
    version='1.0.0',
    packages=['cow', 'cow.patterns','cow.patterns.observer','cow.patterns.singleton'],
    zip_safe=False,
    install_requires=[]
)
